import React from "react";

import { Outlet, NavLink } from "react-router";

const Dashboard = () => {
    return (
        <div>
            Dashboard
        </div>
    );
};

export default Dashboard;
